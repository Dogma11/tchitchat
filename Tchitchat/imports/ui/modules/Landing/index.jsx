import React, { useCallback } from 'react';
import { Meteor } from 'meteor/meteor';



const Landing = ({ user }) => {
    return (
        <div>
            <h1>FEED ME</h1>
        </div>
    );
};

export default Landing