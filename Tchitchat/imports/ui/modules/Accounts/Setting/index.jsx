import React, { useCallback } from 'react';
import { Meteor } from 'meteor/meteor';



const Setting = () => {
    return (
        <div>
            <h1>FEED ME</h1>
        </div>
    );
};

export default Setting