import React, { useCallback } from 'react';
import { Meteor } from 'meteor/meteor';



const Users = ({ user }) => {
    return (
        <div>
            <h1>FEED ME</h1>
        </div>
    );
};

export default Users